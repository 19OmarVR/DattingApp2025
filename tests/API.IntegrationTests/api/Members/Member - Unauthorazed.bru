meta {
  name: Member - Unauthorazed
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/members
  body: none
  auth: bearer
}

headers {
  ~Authorization: {{authToken}}
}

auth:bearer {
  token: 
}

tests {
  test("Should return unauthorized", function(){
    expect(res.getStatus()).to.equal(401);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
